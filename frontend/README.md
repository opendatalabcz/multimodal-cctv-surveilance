# Frontend

Vite + React UI for the CCTV agent. From this directory:

```bash
npm install
npm run dev
```

The app is at http://localhost:5173 and proxies `/api` to FastAPI on port 8000. See the repo [README](../README.md) for the full stack.
